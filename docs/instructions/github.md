# General

When posting PRs, commits, comments to Github, always identify yourself. Use a simple string with your product name and agent name if applicable (e.g. `[Claude/orchestrator]` or `[Claude/best-practices-code-review]`).

When responding to Copilot review feedback, ALWAYS respond in each comment thread, not as a batch comment below the Copilot feedback.
